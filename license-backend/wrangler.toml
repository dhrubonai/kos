name = "kos-license"
main = "worker-d1.js"
compatibility_date = "2024-11-01"

# D1 database binding (created via dashboard or API):
#   wrangler d1 create kos-license-db  ->  paste the database_id below
# Schema auto-creates on first request (licenses / devices / sessions tables).
d1_databases = [
  { binding = "KOS_DB", database_name = "kos-license-db", database_id = "f91296d0-a35a-4cd6-ac76-1db559208708" }
]

# Secret:
#   wrangler secret put ADMIN_PASSWORD
